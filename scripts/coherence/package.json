{
  "name": "ctx-coherence",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Behavioral coherence checks for the ctx dashboard, run against a live isolated instance.",
  "scripts": {
    "coherence": "bash coherence.sh",
    "claims": "node claims.mjs",
    "test": "node --test *.test.mjs"
  },
  "dependencies": {
    "playwright-core": "^1.40.0"
  }
}
